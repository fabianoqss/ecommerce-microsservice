meta {
  name: Login Wrong Password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "bruno.tester@ecommerce.dev",
    "password": "TotallyWrong@123"
  }
}

docs {
  Requires auth-service/Register.bru to have run first (a valid email, wrong password).

  Expected: 401 UNAUTHORIZED — InvalidCredentialsException, same body/status as
  Login Unknown Email.bru.
}
